# Copy this file to a project root to activate mise tools and a Python venv.
# direnv will source it automatically when you cd into the directory.

use mise

# Activate the project's .venv if it exists (created by `uv venv`)
if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi
